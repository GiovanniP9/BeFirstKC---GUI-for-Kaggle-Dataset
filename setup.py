from setuptools import setup, find_packages

setup(
    name='data_analyser',
    version='0.1',
    packages=find_packages(),  # trova automaticamente tutti i pacchetti con __init__.py
    install_requires=[],
)
